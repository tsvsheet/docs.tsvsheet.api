{
  "baseURL": "https://tsvsheet.github.io/docs.tsvsheet.api/",
  "title": "tsvsheet.api",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "The sheet API server: the tsvsheet edit language over HTTP."
  }
}
